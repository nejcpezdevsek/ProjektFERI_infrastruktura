<h1 class="prikaz" style="text-align: center"> Prikaz zajetih podatkov s telefona</h1>
{{#each this}}
    <div class="container p-5 my-5">
        <h3 style="margin-top: -10px">Phone name: {{phone_name}}</h3>
        <div class="prikazSlik">
            <img src="data:image/jpg;base64,{{image}}" alt="?" style="transform:rotate(90deg);" width="200px" height="200px" class="prikazSlik">
        </div>
        <div class="prikazData">
            <p><b>Pospeškometer:</b> {{accelerometer}}</p>
            <p><b>Žiroskop:</b> {{gyroscope}}</p>
            <p><b>Zemljepisna širina:</b> {{latitude}}</p>
            <p><b>Zemljepisna dolžina:</b> {{longitude}}</p>
        </div>
        <div id="{{_id}}">
            <script>
                mapPosition({{latitude}},{{longitude}},{{_id}});
            </script>
        </div>
    </div>
{{/each}}

<script>
    // Initialize and add the map
    function initMap() {
        // The location of Uluru
        var Velenje = {lat: 46.3535777, lng: 15.11233};
        // The map, centered at Uluru
        //var map = new google.maps.Map(
                //document.getElementById('map'), {zoom: 4, center: Velenje});
        // The marker, positioned at Uluru
        //var marker = new google.maps.Marker({position: Velenje, map: map});
    }

    function mapPosition(latitude, longitude, id) {
        console.log(id);
        latitude = parseFloat(latitude);
        longitude = parseFloat(longitude);
        var position = {lat: latitude, lng: longitude};
        var map = new google.maps.Map(
                document.getElementById(id), {zoom: 4, center: position});
        var marker = new google.maps.Marker({position: position, map: map});
    }

</script>
<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCPPXrvm8dTN5VBKtRHi3wPJknhY0YBBzM&callback=initMap">
</script>
